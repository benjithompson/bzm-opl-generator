apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata: { name: role-binding-crane, namespace: ${NAMESPACE} }
subjects: [{ kind: ServiceAccount, name: ${SERVICE_ACCOUNT}, namespace: ${NAMESPACE} }]
roleRef: { kind: Role, name: role-crane, apiGroup: rbac.authorization.k8s.io }
